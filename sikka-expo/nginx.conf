server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Handle SPA routing
    location / {
        try_files $uri /index.html;
    }

    # Error page fallback
    error_page 404 /index.html;
}
